///////////////////////////////////////////////////////////////////////////
// Precompilled headers
///////////////////////////////////////////////////////////////////////////
#include <pch.hpp>

///////////////////////////////////////////////////////////////////////////
// Headers
///////////////////////////////////////////////////////////////////////////
#include <POS/UI/Button/AImageButton.hpp>



///////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////
// Constructors
//

::pos::ui::button::AImageButton::AImageButton(
    ::QMainWindow& window,
    const ::std::string& filename,
    ::std::size_t xPos,
    ::std::size_t yPos,
    ::std::size_t xSize,
    ::std::size_t ySize
) noexcept
    : ::QPushButton{ &window }
{
    this->setIcon(::QIcon{ ("./assets/icon/"s + filename).c_str() });
    this->setIconSize(::QSize{ static_cast<int>(xSize), static_cast<int>(ySize) });
    this->move(static_cast<int>(xPos), static_cast<int>(yPos));
    this->resize(static_cast<int>(xSize), static_cast<int>(ySize));
    this->show();
}
///////////////////////////////////////////////////////////////////////////
::pos::ui::button::AImageButton::AImageButton(
    ::QMainWindow& window,
    const ::std::string& filename,
    const ::std::string& tooltip,
    ::std::size_t xPos,
    ::std::size_t yPos,
    ::std::size_t xSize,
    ::std::size_t ySize
) noexcept
    : AImageButton{ window, filename, xPos, yPos, xSize, ySize }
{
    this->setToolTip(tooltip.c_str());
}



///////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////
// Rule of 5
//

/// \brief Destructor
///
///////////////////////////////////////////////////////////////////////////
::pos::ui::button::AImageButton::~AImageButton() noexcept = default;
